import styled from 'styled-components'

export const ChartDemoStyle = styled.div`
  background: ${({ theme }) => theme.chartBackground};
  fill: ${({ theme }) => theme.chartBarBackground};
  font-size: 40px;
`
